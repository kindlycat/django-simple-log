name: Tests

on:
  - push
  - pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST: localhost
      POSTGRES_DB: postgres
      POSTGRES_PORT: 5432
    strategy:
      matrix:
        include:
#          - name: Python 3.6 / Django 3.2
#            python-version: "3.6"
#            tox-env: py36-dj32

          - name: Python 3.7 / Django 3.2
            python-version: "3.7"
            tox-env: py37-dj32

          - name: Python 3.8 / Django 3.2
            python-version: "3.8"
            tox-env: py38-dj32
#
#          - name: Python 3.9 / Django 3.2
#            python-version: "3.9"
#            tox-env: py39-dj32
#
#          - name: Python 3.10 / Django 3.2
#            python-version: "3.10"
#            tox-env: py310-dj32
#
#          - name: Python 3.8 / Django 4.0
#            python-version: "3.8"
#            tox-env: py38-dj40
#
#          - name: Python 3.9 / Django 4.0
#            python-version: "3.9"
#            tox-env: py39-dj40
#
#          - name: Python 3.10 / Django 4.0
#            python-version: "3.10"
#            tox-env: py310-dj40
#
#          - name: Python 3.8 / Django 4.1
#            python-version: "3.8"
#            tox-env: py38-dj41
#
#          - name: Python 3.9 / Django 4.1
#            python-version: "3.9"
#            tox-env: py39-dj41
#
#          - name: Python 3.10 / Django 4.1
#            python-version: "3.10"
#            tox-env: py310-dj41
#
#          - name: Python 3.10 / Django 4.1
#            python-version: "3.10"
#            tox-env: py310-dj41
#
#          - name: Python 3.11 / Django 4.1
#            python-version: "3.11"
#            tox-env: py311-dj41
#
#          - name: Python 3.8 / Django master
#            python-version: "3.8"
#            tox-env: py38-djmaster
#
#          - name: Python 3.9 / Django master
#            python-version: "3.9"
#            tox-env: py39-djmaster
#
#          - name: Python 3.10 / Django master
#            python-version: "3.10"
#            tox-env: py310-djmaster
#
#          - name: Python 3.10 / Django master
#            python-version: "3.10"
#            tox-env: py310-djmaster
#
#          - name: Python 3.11 / Django master
#            python-version: "3.11"
#            tox-env: py311-djmaster
#
#          - name: Linters
#            python-version: "3.11"
#            tox-env: linters
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_USER: ${{ env.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ env.POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ env.POSTGRES_DB }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox "django>=3.2,<3.3"
          pip install -r requirements.txt
      - name: Test with tox
        env:
          POSTGRES_USER: ${{ env.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ env.POSTGRES_PASSWORD }}
          POSTGRES_HOST: ${{ env.POSTGRES_HOST }}
          POSTGRES_DB: ${{ env.POSTGRES_DB }}
          POSTGRES_PORT: ${{ env.POSTGRES_PORT }}
        run: |
          python ./runtests.py
#          tox -e ${{ matrix.tox-env }}

